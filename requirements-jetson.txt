# Audited Jetson Python environment: L4T R36.5.2, Python 3.10, ARM64.
# Exact pins are deliberate. Update only in a feature commit with device tests.
#
# Install NVIDIA Torch/Torchvision separately from the Jetson-compatible source:
#   torch==2.5.0a0+872d972e41.nv24.8
#   torchvision==0.20.0a0+afc54f7
# Never install generic PyPI Torch on Jetson.
# TensorRT 10.3.0.30 is supplied by JetPack, not pip.

numpy==1.26.4
opencv-python==4.13.0.92
PyYAML==6.0.3
requests==2.34.2

ultralytics==8.4.67
onnx==1.21.0
onnxruntime==1.23.2
insightface==1.0.1

faster-whisper==1.2.1
openwakeword==0.6.0
piper-tts==1.4.2
sounddevice==0.5.5
soundfile==0.14.0

# Required by the current TensorRT runner. It was missing at the audit and must
# be built/installed on-device, then verified before marking the baseline green.
pycuda==2024.1.2
